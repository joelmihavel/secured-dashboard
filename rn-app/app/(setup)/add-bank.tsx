/**
 * Post-Approval Bank Details Screen
 * Renders AddBankForm in default (post-approval) mode.
 */

import AddBankForm from '@/src/components/setup/AddBankForm';

export default function AddBankScreen() {
  return <AddBankForm />;
}
