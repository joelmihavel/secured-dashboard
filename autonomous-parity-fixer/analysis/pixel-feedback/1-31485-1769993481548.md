# Pixel-Perfect Feedback Report

**Screen:** Onboarding / Add bank details
**Figma ID:** 1-31485
**Route:** /(setup)/add-bank
**Generated:** 2026-02-02T00:51:21.547Z

## Summary

| Metric | Value |
|--------|-------|
| Total Issues | 14 |
| Critical | 7 |
| Major | 5 |
| Minor | 2 |
| Files Affected | 3 |

## Batch 0: Structural Analysis (Deterministic)

These issues were detected by analyzing Figma data directly - **visual comparison cannot catch these**:

### Text Alignment Issues (1)

- **Text** (I31:2906;50:322)
  - Figma: `textAlignHorizontal: CENTER`
  - Fix: `Add textAlign: 'center' to the style`

### Text Structure Issues (3)
*Text that should be single line with mixed colors, NOT multiple lines*

- **₹ 32,175** (1:31515)
  - Text: "₹  32,175..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **Let’s get to  know you** (1:31559)
  - Text: "Let’s get to  know you..."
  - Has 4 style segments
  - Fix: Use single `<Text>` with nested spans
- **Didn’t receive the code? Resend** (1:31588)
  - Text: "Didn’t receive the code? Resend..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans

### Button Fill Issues (8)
*Buttons with specific fill colors - verify RN backgroundColor matches*

- **Rectangle 52** (1:31490)
  - Button text: "Rectangle 52"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 53** (1:31492)
  - Button text: "Rectangle 53"
  - Figma fill: `#D9D9D9`
  - Action: Verify button has backgroundColor: '#D9D9D9' or equivalent design token
- **Frame 1686557209** (1:31536)
  - Button text: "Pay Now"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 140** (I1:31564;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **Rectangle 54** (1:31566)
  - Button text: "Rectangle 54"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 55** (1:31567)
  - Button text: "Rectangle 55"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 53** (1:31569)
  - Button text: "Rectangle 53"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **button** (1:31587)
  - Button text: "Proceed"
  - Figma fill: `#202020`
  - Action: Verify button has backgroundColor: '#202020' or equivalent design token

### Position Issues (2)
*Elements with specific offsets requiring absolute positioning*

- **Frame 1686557300** (1:31491)
  - Relative position: left=0.0, top=899.0
  - Fix: Add absolute positioning with exact coordinates
- **Hint text** (I31:2906;50:326)
  - Relative position: left=0.0, top=96.0
  - Fix: Add absolute positioning with exact coordinates

## Consolidated Fixes

### Critical Issues (Fix Immediately)

#### backgroundColor in `src/components/onboarding/shared.tsx`
**Lines:** N/A
**Problem:** Multiple button-like elements (nodes 1:31490, 1:31536, 1:31566, 1:31567) have a fill of #000000 in Figma, which is not a design token. This should be implemented as a background color in React Native. Ensure these components use a black background.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
backgroundColor: theme.colors.black[500], // or another color

// After:
backgroundColor: '#000000',
```

#### backgroundColor in `src/components/onboarding/shared.tsx`
**Lines:** N/A
**Problem:** A button-like element (node 1:31492) has a fill of #D9D9D9 in Figma. This should be mapped to the `neutral.200` design token (#DDDDDD) for consistency.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
backgroundColor: theme.colors.neutral[300], // or another color

// After:
backgroundColor: theme.colors.neutral[200], // #DDDDDD
```

#### backgroundColor in `src/components/onboarding/shared.tsx`
**Lines:** N/A
**Problem:** Multiple button-like elements (nodes I1:31564;137:37, 1:31569) have a fill of #4D4D4D in Figma. This corresponds to the `black.400` design token and should be used for the component's background color.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
backgroundColor: theme.colors.black[500], // or another color

// After:
backgroundColor: theme.colors.black[400],
```

#### backgroundColor in `app/auth/verify-otp.tsx`
**Lines:** N/A
**Problem:** The 'Proceed' button (node 1:31587) has a fill of #202020 in Figma. This must be mapped to the `black.500` design token for its background color.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
backgroundColor: theme.colors.primary, // or another color

// After:
backgroundColor: theme.colors.black[500],
```

#### Text Structure in `src/components/onboarding/shared.tsx`
**Lines:** N/A
**Problem:** The text '₹ 32,175' (node 1:31515) is a single line in Figma with two different colors. It must be rendered using a single <Text> component with a nested <Text> span to apply the different styles, not as two separate Text components.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<View>
  <Text style={styles.symbol}>₹</Text>
  <Text style={styles.amount}>32,175</Text>
</View>

// After:
<Text style={styles.baseText}>
  <Text style={styles.symbol}>₹  </Text>
  <Text style={styles.amount}>32,175</Text>
</Text>
```

#### Text Structure in `src/components/onboarding/shared.tsx`
**Lines:** N/A
**Problem:** The heading 'Let’s get to know you' (node 1:31559) is a single line in Figma with multiple colors. It must be rendered using a single <Text> component with nested <Text> spans for each style, preventing incorrect line breaks.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<>
  <Text>Let’s get to</Text>
  <Text>know you</Text>
</>

// After:
<Text style={styles.baseText}>
  <Text style={styles.blackPart}>Let’s get to </Text>
  <Text style={styles.orangePart}>know you</Text>
</Text>
```

#### Text Structure in `app/auth/verify-otp.tsx`
**Lines:** N/A
**Problem:** The text 'Didn’t receive the code? Resend' (node 1:31588) is a single line in Figma with two different styles ('Resend' is a link). It must be rendered using a single <Text> component with a nested <Text> span for the link part to ensure it stays on one line.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<View style={{ flexDirection: 'row' }}>
  <Text>Didn’t receive the code? </Text>
  <TouchableOpacity><Text>Resend</Text></TouchableOpacity>
</View>

// After:
<Text style={styles.baseText}>
  Didn’t receive the code?{' '}
  <Text style={styles.linkText}>Resend</Text>
</Text>
```

### Major Issues

- **Background Overlay** in `app/auth/verify-otp.tsx`: The screen requires a dual-layer background overlay for the modal. The first layer is a black tint with 40% opacity. The second layer provides a background blur (radius 8) with 60% opacity. This is a combination of structural issues (nodes 1:31566, 1:31567) and visual feedback, and likely requires a library like `@react-native-community/blur`.
  - Fix: `{
  isModalVisible && (
    <>
      {/* Layer 2: Blur with 60% opacity */}
      <BlurView style={styles.blurOverlay} blurType="dark" blurAmount={8} />
      {/* Layer 1: Black tint with 40% opacity */}
      <View style={styles.darkOverlay} />
    </>
  )
}

// styles.js
blurOverlay: {
  ...StyleSheet.absoluteFillObject,
  zIndex: 1,
  backgroundColor: 'rgba(0, 0, 0, 0.6)',
},
darkOverlay: {
  ...StyleSheet.absoluteFillObject,
  zIndex: 2,
  backgroundColor: 'rgba(0, 0, 0, 0.4)',
}`
- **textAlign** in `src/components/onboarding/OTPInput.tsx`: The text inside the OTP input boxes (node I31:2906;50:322) has `textAlignHorizontal: CENTER` in Figma. The corresponding React Native style must be `textAlign: 'center'`.
  - Fix: `input: {
  // ... other styles
  textAlign: 'center',
}`
- **position** in `src/components/onboarding/shared.tsx`: A frame component (node 1:31491) is positioned at (0, 899) relative to its parent. This requires absolute positioning to be rendered correctly.
  - Fix: `container: {
  position: 'absolute',
  left: 0,
  top: 899,
}`
- **position** in `src/components/onboarding/OTPInput.tsx`: The 'Hint text' (node I31:2906;50:326) within the OTP component is positioned at (0, 96) relative to its parent. This requires absolute positioning to be placed correctly below the input fields.
  - Fix: `hintText: {
  position: 'absolute',
  left: 0,
  top: 96,
}`
- **paddingHorizontal** in `app/auth/verify-otp.tsx`: The main content container of the modal has a horizontal padding of 48px in Figma. This should be updated to use the `xxxl` spacing token.
  - Fix: `contentContainer: {
  paddingHorizontal: theme.spacing.xxxl, // 48px
}`

### Minor Issues

- **gap** in `app/auth/verify-otp.tsx`: The vertical spacing between the OTP input group and the 'Wrong Code' error message should be 16px, which corresponds to the `md` spacing token.
- **gap** in `app/auth/verify-otp.tsx`: The vertical spacing between the subtitle text and the OTP input block is a specific 30.38px in Figma. This should be implemented as a margin of 30.