# Pixel-Perfect Feedback Report

**Screen:** Onboarding / Invite Landlord
**Figma ID:** 1-31671
**Route:** /(setup)/invite-landlord
**Generated:** 2026-02-02T01:07:03.593Z

## Summary

| Metric | Value |
|--------|-------|
| Total Issues | 13 |
| Critical | 3 |
| Major | 3 |
| Minor | 0 |
| Files Affected | 1 |

## Batch 0: Structural Analysis (Deterministic)

These issues were detected by analyzing Figma data directly - **visual comparison cannot catch these**:

### Text Structure Issues (3)
*Text that should be single line with mixed colors, NOT multiple lines*

- **₹ 32,175** (1:31701)
  - Text: "₹  32,175..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **Let’s get to  know you** (1:31745)
  - Text: "Let’s get to  know you..."
  - Has 4 style segments
  - Fix: Use single `<Text>` with nested spans
- **I consent to a one-time verification to confirm my profile details. Verification is securely handled via Cashfree** (1:31751)
  - Text: "I consent to a one-time verification to confirm my..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans

### Button Fill Issues (6)
*Buttons with specific fill colors - verify RN backgroundColor matches*

- **Rectangle 52** (1:31676)
  - Button text: "Rectangle 52"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 53** (1:31678)
  - Button text: "Rectangle 53"
  - Figma fill: `#D9D9D9`
  - Action: Verify button has backgroundColor: '#D9D9D9' or equivalent design token
- **Frame 1686557209** (1:31722)
  - Button text: "Pay Now"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **button** (1:31748)
  - Button text: "Get Started"
  - Figma fill: `#202020`
  - Action: Verify button has backgroundColor: '#202020' or equivalent design token
- **Rectangle 1** (I1:31750;124:6249)
  - Button text: "Rectangle 1"
  - Figma fill: `#131313`
  - Action: Verify button has backgroundColor: '#131313' or equivalent design token
- **Rectangle 5** (I1:31750;124:6257)
  - Button text: "Rectangle 5"
  - Figma fill: `#141618`
  - Action: Verify button has backgroundColor: '#141618' or equivalent design token

### Position Issues (2)
*Elements with specific offsets requiring absolute positioning*

- **image 149** (237:2741)
  - Relative position: left=-463.0, top=-747.0
  - Fix: Add absolute positioning with exact coordinates
- **Frame 1686557300** (1:31677)
  - Relative position: left=0.0, top=899.0
  - Fix: Add absolute positioning with exact coordinates

## Consolidated Fixes

### Critical Issues (Fix Immediately)

#### Text Structure in `app/auth/onboarding-screen.tsx`
**Lines:** N/A
**Problem:** Three text elements on this screen have mixed styling (e.g., different colors within the same line). They are being rendered incorrectly, likely as separate <Text> components, which breaks the single-line flow seen in Figma. To fix this, use a single parent <Text> component with nested <Text> components for each style segment.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
{
  /* Example of incorrect structure */
  <Text style={styles.textGray}>I consent to a one-time verification...</Text>
  <Text style={styles.textWhite}>Verification is securely handled via Cashfree</Text>
}

// After:
{
  /* Correct structure for multi-styled text */
  <Text style={styles.baseText}>
    <Text style={styles.textGray}>I consent to a one-time verification to confirm my profile details. </Text>
    <Text style={styles.textWhite}>Verification is securely handled via Cashfree</Text>
  </Text>
}
```

#### backgroundColor in `app/auth/onboarding-screen.tsx`
**Lines:** N/A
**Problem:** The 'Get Started' button has an incorrect background color. Figma specifies #202020, which maps to the design token `colors.black[500]`.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
style={{ backgroundColor: /* incorrect color */ }}

// After:
style={{ backgroundColor: _designTokens.colors.black[500] }}
```

#### backgroundColor in `app/auth/onboarding-screen.tsx`
**Lines:** N/A
**Problem:** Multiple other buttons and interactive elements have background colors that mismatch the Figma design. Please verify the following:
- 'Pay Now' button (node 1:31722) should have a background of '#000000'.
- Consent checkbox background (node I1:31750;124:6249) should be `_designTokens.colors.black[700]` (#131313).
- Another button element (node I1:31750;124:6257) should be `_designTokens.colors.black[700]` (#141618 is close to #131313).
- A button-like rectangle (node 1:31678) should be `_designTokens.colors.neutral[200]` (#D9D9D9 is close to #DDDDDD).
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
/* Background colors for various buttons are incorrect */

// After:
/* 
  Ensure the following background colors are applied:
  - 'Pay Now' button: '#000000'
  - Consent checkbox: _designTokens.colors.black[700]
  - Other elements as specified in the explanation.
*/
```

### Major Issues

- **Layout & Background** in `app/auth/onboarding-screen.tsx`: The screen background is not a solid color. It's a multi-layered composition requiring a base color, a large texture image, and a top gradient image, all positioned absolutely. The texture image (image 149) is specifically positioned relative to its parent container.
  - Fix: `<View style={{ flex: 1, backgroundColor: _designTokens.colors.black[700] }}>
  {/* Top gradient glow */}
  <Image
    source={require('path/to/background-shape-160-2927.png')}
    style={{ position: 'absolute', top: 0, left: -44, width: 481, height: 405, zIndex: 0 }}
  />
  {/* Texture overlay - position from structural analysis */}
  <Image
    source={require('path/to/texture-image-237-2741.png')}
    style={{ position: 'absolute', top: -747, left: -463, opacity: 0.08, zIndex: 0 }}
  />
  {/* Screen content on top */}
  <SafeAreaView style={{ flex: 1, zIndex: 1 }}>
    {/* ... rest of the screen content ... */}
  </SafeAreaView>
</View>`
- **style** in `app/auth/onboarding-screen.tsx`: The main content container has incorrect padding and vertical spacing. The container should have 48px of horizontal and top padding, and the vertical gap between major content blocks should also be 48px.
  - Fix: `const styles = StyleSheet.create({
  contentContainer: {
    paddingHorizontal: _designTokens.spacing.xxxl, // 48px
    paddingTop: _designTokens.spacing.xxxl, // 48px
    gap: _designTokens.spacing.xxxl, // 48px
  }
});`
- **position** in `app/auth/onboarding-screen.tsx`: A specific UI element, 'Frame 1686557300', is designed to be positioned 899px from the top of its parent container. This requires absolute positioning to ensure it appears at the correct location, likely near the bottom of the screen.
  - Fix: `const styles = StyleSheet.create({
  footerElement: {
    position: 'absolute',
    left: 0,
    top: 899,
    width: '100%'
    // ... other styles
  }
});`
