# Pixel-Perfect Feedback Report

**Screen:** Splash / get-started --animation
**Figma ID:** 1-28055
**Route:** /(auth)/splash
**Generated:** 2026-02-01T23:52:16.224Z

## Summary

| Metric | Value |
|--------|-------|
| Total Issues | 5 |
| Critical | 1 |
| Major | 4 |
| Minor | 0 |
| Files Affected | 1 |

## Batch 0: Structural Analysis (Deterministic)

These issues were detected by analyzing Figma data directly - **visual comparison cannot catch these**:

### Text Structure Issues (2)
*Text that should be single line with mixed colors, NOT multiple lines*

- **Main Heading** (1:28066)
  - Text: "Make  your rent  work for you→..."
  - Has 4 style segments
  - Fix: Use single `<Text>` with nested spans
- **Login Text** (1:28070)
  - Text: "Already a user? Log in..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans

### Button Fill Issues (1)
*Buttons with specific fill colors - verify RN backgroundColor matches*

- **Rectangle 140** (I1:28069;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token

### Position Issues (1)
*Elements with specific offsets requiring absolute positioning*

- **image 149** (237:2727)
  - Relative position: left=-463.0, top=-747.0
  - Fix: Add absolute positioning with exact coordinates

## Consolidated Fixes

### Critical Issues (Fix Immediately)

#### Vertical Alignment & Sizing in `app/auth/get-started.tsx`
**Lines:** N/A
**Problem:** The main content container is incorrectly centered. It should be aligned to the bottom of the screen with a fixed height of 613px, and its internal elements (heading vs buttons) should be spaced apart to fill that height. This requires a nested flexbox structure.
**Figma:** Main content block has a fixed height of 613px, is aligned to the bottom of the screen, and uses 'space-between' for its children. | **Current:** The main container uses 'flex: 1' and 'justifyContent: 'center'', which incorrectly centers all content vertically.

```typescript
// Before:
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    paddingHorizontal: 48,
  },
});

// After:
const styles = StyleSheet.create({
  outerContainer: {
    flex: 1,
    justifyContent: 'flex-end', // Pushes the main content block to the bottom
  },
  mainContent: {
    height: 613, // Fixed height as per Figma
    justifyContent: 'space-between', // Spreads content within the block
    paddingBottom: 64, // spacing.huge
  },
  // Apply horizontal padding to child sections, not the main container
  topSection: {
    paddingHorizontal: 48, // spacing.xxxl
  },
  bottomSection: {
    paddingHorizontal: 48, // spacing.xxxl
  },
});
```

### Major Issues

- **Text Structure** in `app/auth/get-started.tsx`: The main heading 'Make your rent work for you→' is a single line in Figma with multiple colors. It must be rendered using a single <Text> component with nested <Text> spans for styling, not as multiple separate Text components which would break the layout and line wrapping.
  - Fix: `<Text style={styles.headingBase}>
  Make your rent work for you
  <Text style={styles.headingArrow}>→</Text>
</Text>`
- **Background Layering & Positioning** in `app/auth/get-started.tsx`: The background texture image (node 237:2727) and top gradient are not positioned correctly. They should be layered behind all UI content using absolute positioning, rather than being in the normal layout flow where they would push content down. This combines a structural position issue with a screen-level layering problem.
  - Fix: `<View style={styles.screenContainer}>
  <Image 
    source={require('.../texture.png')} 
    style={styles.backgroundImage} 
  />
  <Image 
    source={require('.../gradient.png')} 
    style={styles.backgroundGradient} 
  />
  <SafeAreaView style={{flex: 1}}>
    {/* Main content goes here */}
  </SafeAreaView>
</View>

const styles = StyleSheet.create({
  screenContainer: {
    flex: 1,
    backgroundColor: '#131313', // colors.black[700]
  },
  backgroundImage: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    opacity: 0.08,
  },
  backgroundGradient: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    height: 405, // From Figma node 1:28057
  },
});`
- **Text Structure** in `app/auth/get-started.tsx`: The login text 'Already a user? Log in' is a single line in Figma with two different styles. It must be rendered using a single <Text> component with a nested <Text> span for the 'Log in' part to ensure correct alignment and wrapping.
  - Fix: `<Text style={styles.loginTextBase}>
  Already a user?{' '}
  <Text style={styles.loginTextLink}>Log in</Text>
</Text>`
- **Button Background Color** in `app/auth/get-started.tsx`: The 'Get Started' button's background color does not match the Figma design. The fill color in Figma is #4D4D4D.
  - Fix: `backgroundColor: '#4D4D4D', // colors.black[400]`
