# Pixel-Perfect Feedback Report

**Screen:** auth / sign up --enter OTP
**Figma ID:** 1-31175
**Route:** /(auth)/otp
**Generated:** 2026-02-02T00:26:02.115Z

## Summary

| Metric | Value |
|--------|-------|
| Total Issues | 7 |
| Critical | 3 |
| Major | 4 |
| Minor | 0 |
| Files Affected | 3 |

## Batch 0: Structural Analysis (Deterministic)

These issues were detected by analyzing Figma data directly - **visual comparison cannot catch these**:

### Text Alignment Issues (1)

- **Text** (I1:31272;50:322)
  - Figma: `textAlignHorizontal: CENTER`
  - Fix: `Add textAlign: 'center' to the style`

### Text Structure Issues (3)
*Text that should be single line with mixed colors, NOT multiple lines*

- **₹ 32,175** (1:31205)
  - Text: "₹  32,175..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **Let’s get to  know you** (1:31249)
  - Text: "Let’s get to  know you..."
  - Has 4 style segments
  - Fix: Use single `<Text>` with nested spans
- **Didn’t receive the code? Resend** (1:31275)
  - Text: "Didn’t receive the code? Resend..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans

### Button Fill Issues (7)
*Buttons with specific fill colors - verify RN backgroundColor matches*

- **Rectangle 52** (1:31180)
  - Button text: "Rectangle 52"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 53** (1:31182)
  - Button text: "Rectangle 53"
  - Figma fill: `#D9D9D9`
  - Action: Verify button has backgroundColor: '#D9D9D9' or equivalent design token
- **Frame 1686557209** (1:31226)
  - Button text: "Pay Now"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 140** (I1:31254;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **Rectangle 54** (1:31256)
  - Button text: "Rectangle 54"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 53** (1:31258)
  - Button text: "Rectangle 53"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **button** (1:31274)
  - Button text: "Proceed"
  - Figma fill: `#202020`
  - Action: Verify button has backgroundColor: '#202020' or equivalent design token

### Position Issues (2)
*Elements with specific offsets requiring absolute positioning*

- **Frame 1686557300** (1:31181)
  - Relative position: left=0.0, top=899.0
  - Fix: Add absolute positioning with exact coordinates
- **Hint text** (I1:31272;50:326)
  - Relative position: left=0.0, top=96.0
  - Fix: Add absolute positioning with exact coordinates

## Consolidated Fixes

### Critical Issues (Fix Immediately)

#### undefined in `app/(auth)/sign-up-otp.tsx`
**Lines:** N/A
**Problem:** The screen's core structure is a modal overlay. This requires a specific layering of components: the background content, a full-screen semi-transparent blur overlay, and an absolutely positioned bottom sheet. The bottom sheet also needs correct styling (rounded corners, background color) and must account for the safe area at the bottom.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
/* Likely a simple <View> or <ScrollView> structure */
<View>
  {/* Background content... */}
  {/* Bottom sheet content... */}
</View>

// After:
import { BlurView } from 'expo-blur';
import { useSafeAreaInsets } from 'react-native-safe-area-context';

// ... in component
const insets = useSafeAreaInsets();

return (
  <View style={styles.container}>
    {/* 1. Background Content */}
    <View style={styles.backgroundContent}>
      {/* ... your existing background UI ... */}
    </View>

    {/* 2. Blur Overlay */}
    <BlurView intensity={10} tint="dark" style={StyleSheet.absoluteFillObject} />

    {/* 3. Bottom Sheet */}
    <View style={[styles.bottomSheet, { paddingBottom: insets.bottom }]}>
      {/* ... OTP entry content ... */}
    </View>
  </View>
);

// ... in StyleSheet
const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  backgroundContent: {
    flex: 1,
    // ... styles for background
  },
  bottomSheet: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0,
    backgroundColor: colors.black[600], // #1A1A1A
    borderTopLeftRadius: radius.xl, // 24
    borderTopRightRadius: radius.xl, // 24
    // padding is handled below
  },
});
```

#### undefined in `app/components/auth/MultiStyleText.tsx`
**Lines:** N/A
**Problem:** The text "Didn’t receive the code? Resend" uses two different colors on a single line. It must be implemented with nested <Text> components to prevent unwanted line breaks and apply correct styling.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<Text>Didn’t receive the code? Resend</Text>

// After:
<Text style={styles.baseText}>
  <Text style={styles.regularPart}>Didn’t receive the code? </Text>
  <Text style={styles.highlightPart}>Resend</Text>
</Text>

// styles.regularPart -> color: e.g., colors.neutral[500]
// styles.highlightPart -> color: e.g., colors.brand[500]
```

#### undefined in `app/(auth)/sign-up-otp.tsx`
**Lines:** N/A
**Problem:** Text elements in the background content, "₹ 32,175" and "Let’s get to know you", use multiple styles on a single line in Figma. They must be rendered using nested <Text> components to ensure they appear on one line with the correct styling.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<Text>₹ 32,175</Text>
<Text>Let’s get to know you</Text>

// After:
/* For the price text */
<Text style={...}>
  <Text style={{ color: colors.neutral[500] }}>₹  </Text>
  <Text style={{ color: colors.white }}>32,175</Text>
</Text>

/* For the header text */
<Text style={...}>
  <Text style={{ color: colors.white }}>Let’s get to </Text>
  <Text style={{ color: colors.brand[500] }}>know you</Text>
</Text>
```

### Major Issues

- **undefined** in `app/(auth)/sign-up-otp.tsx`: The content container within the bottom sheet requires horizontal padding of 48px and a vertical gap of ~32px between its child elements (header, OTP input, button).
  - Fix: `/* <View style={styles.bottomSheetContent}>... */

const styles = StyleSheet.create({
  bottomSheetContent: {
    paddingHorizontal: spacing.xxxl, // 48px
    gap: spacing.xl, // 32px
    paddingTop: spacing.xl, // Add top padding as well
  }
});`
- **undefined** in `app/components/auth/OTPInput.tsx`: The OTP input's placeholder text ("- - - -") should be center-aligned. Additionally, the hint text below it must be absolutely positioned relative to the input container.
  - Fix: `/* In your OTP input component's StyleSheet */
const styles = StyleSheet.create({
  otpInput: {
    // ... other styles
    textAlign: 'center',
  },
  hintTextContainer: {
    // This is the container for the hint text
    position: 'absolute',
    top: 96, // As per Figma, adjust if needed based on input height
    left: 0,
    right: 0,
    alignItems: 'center',
  }
});`
- **undefined** in `app/(auth)/sign-up-otp.tsx`: The 'Proceed' button in the bottom sheet has an incorrect background color. It should be #202020.
  - Fix: `backgroundColor: colors.black[500], // #202020`
- **undefined** in `app/(auth)/sign-up-otp.tsx`: Several interactive elements in the background content have incorrect fill colors. Update them to match the design specification.
  - Fix: `/* For node 'Rectangle 52' (1:31180) and 'Pay Now' (1:31226) */
style={{ backgroundColor: '#000000' }}

/* For node 'Rectangle 53' (1:31182) */
style={{ backgroundColor: '#D9D9D9' }}

/* For nodes 'Rectangle 140' (I1:31254;137:37) and 'Rectangle 53' (1:31258) */
style={{ backgroundColor: colors.black[400] }} // #4D4D4D`
