# Pixel-Perfect Feedback Report

**Screen:** Onboarding / Waitlist Screen
**Figma ID:** 41-11206
**Route:** /(waitlist)/index
**Generated:** 2026-02-02T01:43:20.621Z

## Summary

| Metric | Value |
|--------|-------|
| Total Issues | 16 |
| Critical | 6 |
| Major | 9 |
| Minor | 1 |
| Files Affected | 5 |

## Batch 0: Structural Analysis (Deterministic)

These issues were detected by analyzing Figma data directly - **visual comparison cannot catch these**:

### Text Alignment Issues (5)

- **Application Sent** (41:11226)
  - Figma: `textAlignHorizontal: CENTER`
  - Fix: `Add textAlign: 'center' to the style`
- **Text** (I41:11253;50:331;1106:66617)
  - Figma: `textAlignHorizontal: CENTER`
  - Fix: `Add textAlign: 'center' to the style`
- **Text** (I41:11253;50:332;1106:66617)
  - Figma: `textAlignHorizontal: CENTER`
  - Fix: `Add textAlign: 'center' to the style`
- **Text** (I41:11253;50:333;1106:66617)
  - Figma: `textAlignHorizontal: CENTER`
  - Fix: `Add textAlign: 'center' to the style`
- **Text** (I41:11253;50:334;1106:66617)
  - Figma: `textAlignHorizontal: CENTER`
  - Fix: `Add textAlign: 'center' to the style`

### Text Structure Issues (4)
*Text that should be single line with mixed colors, NOT multiple lines*

- **Welcome,  Rishabh Agnihotri** (41:11218)
  - Text: "Welcome,   Rishabh Agnihotri..."
  - Has 3 style segments
  - Fix: Use single `<Text>` with nested spans
- **18 / 150  members onboarded** (41:11246)
  - Text: "18 / 150  members onboarded..."
  - Has 3 style segments
  - Fix: Use single `<Text>` with nested spans
- **What do you get with Flent Secured?** (41:11258)
  - Text: "What do you get with Flent Secured?..."
  - Has 3 style segments
  - Fix: Use single `<Text>` with nested spans
- **What’s  Coming your way?** (41:11291)
  - Text: "What’s  Coming your way?..."
  - Has 3 style segments
  - Fix: Use single `<Text>` with nested spans

### Button Fill Issues (3)
*Buttons with specific fill colors - verify RN backgroundColor matches*

- **Rectangle 140** (I41:11254;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **Rectangle 53** (41:11279)
  - Button text: "Rectangle 53"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **Rectangle 140** (I41:11311;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token

### Position Issues (4)
*Elements with specific offsets requiring absolute positioning*

- **image 149** (237:2761)
  - Relative position: left=-463.0, top=-747.0
  - Fix: Add absolute positioning with exact coordinates
- **Frame 2095586317** (41:11278)
  - Relative position: left=0.0, top=1803.0
  - Fix: Add absolute positioning with exact coordinates
- **Hint text** (41:11249)
  - Relative position: left=17.5, top=-28.4
  - Fix: Add absolute positioning with exact coordinates
- **Hint text** (I41:11253;50:335)
  - Relative position: left=0.0, top=116.0
  - Fix: Add absolute positioning with exact coordinates

## Consolidated Fixes

### Critical Issues (Fix Immediately)

#### background in `app/(waitlist)/index.tsx`
**Lines:** 333,336
**Problem:** The screen's root View is missing the decorative background images from the design. Two absolutely positioned Image components should be added behind the ScrollView: one for the top gradient overlay and another for the dotted pattern, which should have low opacity.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
screen: {
  flex: 1,
  backgroundColor: colors.black[700],
}

// After:
screen: {
  flex: 1,
  backgroundColor: colors.black[700],
},
backgroundContainer: {
  ...StyleSheet.absoluteFillObject,
  zIndex: -1,
},
gradientOverlay: {
  position: 'absolute',
  top: 0,
  left: 0,
  right: 0,
  height: 500, // Adjust as needed
},
patternOverlay: {
  ...StyleSheet.absoluteFillObject,
  opacity: 0.08,
}

// In your component's JSX:
// <View style={styles.screen}>
//   <View style={styles.backgroundContainer}>
//     <Image source={require('path/to/gradient.png')} style={styles.gradientOverlay} />
//     <Image source={require('path/to/pattern.png')} style={styles.patternOverlay} resizeMode='repeat' />
//   </View>
//   <ScrollView ... >
```

#### text-structure in `app/(waitlist)/index.tsx`
**Lines:** 306,328
**Problem:** The welcome title ('Welcome, Rishabh Agnihotri') is implemented as two separate Text components, but Figma specifies it as a single text element with a line break and different colors. This should be refactored into a single Text component with a nested Text component for the accent color part to ensure correct text flow and semantic structure.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<View>
  <Text style={styles.titleGray}>
    Welcome,
  </Text>
  <Text style={styles.titleAccent}>
    {displayName}
  </Text>
</View>

// After:
<Text style={styles.titleGray}>
  Welcome,{"\n"}
  <Text style={styles.titleAccent}>{displayName}</Text>
</Text>
```

#### text-structure in `app/(waitlist)/index.tsx`
**Lines:** 150,160
**Problem:** The text '18 / 150 members onboarded' is a single line in Figma with multiple colors. It should be implemented as a single Text component with nested Text components for styling different parts, not as separate elements.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
// Likely implemented as separate <Text> components or with incorrect styling
<Text style={styles.progressText}>18 / 150 members onboarded</Text>

// After:
<Text style={styles.baseProgressText}>
  <Text style={styles.highlightProgressText}>18 / 150</Text>
  <Text> members onboarded</Text>
</Text>
```

#### styles in `src/components/waitlist/ApplicationTimeline.tsx`
**Lines:** 201,205
**Problem:** The main container for the timeline is missing its card-like styling. It needs a background color, border radius, and padding to match the Figma design.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
container: {
  width: '100%',
  gap: spacing.lg, // 24
},

// After:
container: {
  width: '100%',
  backgroundColor: colors.black[500],
  borderRadius: radius.md, // 12
  paddingVertical: spacing.lg, // 24
  paddingHorizontal: spacing.md, // 16
  gap: spacing.lg, // 24
},
```

#### text-structure in `src/components/waitlist/BenefitsCard.tsx`
**Lines:** 50,60
**Problem:** The title 'What do you get with Flent Secured?' is a single text element in Figma with multiple colors. It should be implemented using a single Text component with a nested Text component for the accent-colored phrase 'Flent Secured?'.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<Text style={styles.title}>What do you get with Flent Secured?</Text>

// After:
<Text style={styles.title}>
  What do you get with{'\n'}
  <Text style={styles.titleAccent}>Flent Secured?</Text>
</Text>
```

#### text-structure in `src/components/waitlist/FeaturesCard.tsx`
**Lines:** 50,60
**Problem:** The title 'What’s Coming your way?' is a single text element in Figma with multiple colors. It should be implemented using a single Text component with a nested Text component for the accent-colored phrase 'Coming your way?'.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<Text style={styles.title}>What’s Coming your way?</Text>

// After:
<Text style={styles.title}>
  What’s{'\n'}
  <Text style={styles.titleAccent}>Coming your way?</Text>
</Text>
```

### Major Issues

- **gap** in `app/(waitlist)/index.tsx`: The layout of the invite card is incorrect due to a single uniform gap. The gap between the 'Have an Invite Code?' label and its description should be 4px, and the gap between that text block and the input below should be 24px. This requires removing the `inviteSection` wrapper, applying the 24px gap to the parent card, and wrapping the two text labels in a new View with a 4px gap. The divider's `marginTop` should also be removed as the parent gap now controls the spacing.
  - Fix: `// 1. Remove the `inviteSection` style and its wrapping <View> in the JSX.
// 2. Add `gap: spacing.lg` (24px) to the parent `inviteCard` style.
// 3. Wrap the two invite text labels in a new View like this:
//    <View style={{ gap: spacing.xxs, width: '100%' }}> ... </View>
// 4. Update the divider style:
divider: {
  width: '100%',
  height: 1,
  backgroundColor: colors.neutral[800],
  borderRadius: radius.full,
  // marginTop: spacing.lg, // REMOVED
},`
- **color** in `app/(waitlist)/index.tsx`: The 'Welcome,' text color is too dark compared to the design. It is currently using `colors.neutral[500]` (#A9A9A9), but should be a much lighter gray to match the visual hierarchy in Figma.
  - Fix: `titleGray: {
  ...typography.h3,
  color: colors.neutral[200], // Changed from neutral[500]
},`
- **backgroundColor** in `app/(waitlist)/index.tsx`: The disabled state of the primary button has the wrong background color. Figma specifies a fill of #4D4D4D for disabled buttons.
  - Fix: `disabledStyle: {
  backgroundColor: colors.black[400], // #4D4D4D
}`
- **textAlign** in `src/components/waitlist/ApplicationTimeline.tsx`: Multiple text elements within the timeline, including the 'Application Sent' title and the numeric progress indicators, are center-aligned in Figma but are missing `textAlign: 'center'` in the code.
  - Fix: `title: {
  ...typography.bodyLg,
  color: colors.white,
  textAlign: 'center',
},
otpText: {
  ...typography.bodyLg,
  color: colors.white,
  textAlign: 'center',
}`
- **backgroundColor** in `src/components/waitlist/ApplicationTimeline.tsx`: The connector line extending downwards from a timeline item (`connectorBottom`) is incorrectly colored based on the previous item's status. It should be colored based on the current item's status ('complete' or 'active' vs. 'pending').
  - Fix: `<View style={[styles.connectorBottom, { backgroundColor: (status === 'complete' || status === 'active') ? colors.success.approved : colors.neutral[800] }]} />`
- **paddingBottom** in `src/components/waitlist/ApplicationTimeline.tsx`: The `textColumn` style has an unnecessary `paddingBottom` of 16. The parent container's `gap` property already handles the spacing between items. Removing this extra padding ensures consistent vertical spacing.
  - Fix: `textColumn: {
  flex: 1,
  gap: spacing.xs,
  justifyContent: 'center',
},`
- **gap** in `src/components/waitlist/BenefitsCard.tsx`: The vertical gap between the title section and the list of benefits is 24px, but the Figma design specifies a 30px gap. This should be updated to match the design.
  - Fix: `gap: 30,`
- **position** in `src/components/waitlist/ReferralCodeInput.tsx`: The hint/error text below the input field is not correctly positioned. It should be absolutely positioned relative to the input's container to ensure it doesn't affect the layout of other elements, matching its offset in Figma.
  - Fix: `hintText: {
  // ... text styles
  position: 'absolute',
  top: 60, // Adjust based on input height + desired gap
  left: 0,
}`
- **color** in `src/components/waitlist/ReferralCodeInput.tsx`: The error border color for the input is hardcoded as a hex value (#E5484D). It should use the `colors.error` design token to maintain consistency with the design system.
  - Fix: `borderColor: isError ? colors.error : colors.brand[500],`

### Minor Issues

- **height** in `src/components/waitlist/ReferralCodeInput.tsx`: The TextInput has `height: '100%'`, which can cause inconsistent vertical text alignment on Android. Removing this property allows the parent's flex properties (`alignItems: 'center'`) to center the text more reliably across platforms.