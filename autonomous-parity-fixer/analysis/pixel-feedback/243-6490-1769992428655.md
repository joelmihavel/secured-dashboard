# Pixel-Perfect Feedback Report

**Screen:** Home --Empty State --setup payment
**Figma ID:** 243-6490
**Route:** /(main)/index
**Generated:** 2026-02-02T00:33:48.653Z

## Summary

| Metric | Value |
|--------|-------|
| Total Issues | 7 |
| Critical | 1 |
| Major | 5 |
| Minor | 0 |
| Files Affected | 2 |

## Batch 0: Structural Analysis (Deterministic)

These issues were detected by analyzing Figma data directly - **visual comparison cannot catch these**:

### Text Structure Issues (12)
*Text that should be single line with mixed colors, NOT multiple lines*

- **₹ 32,500** (243:6506)
  - Text: "₹  32,500..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **Your rent is due in 10 days** (243:6511)
  - Text: "Your rent is due in 10 days..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **•••• 2341** (243:6523)
  - Text: "•••• 2341..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **EXPIRY 06/26** (243:6525)
  - Text: "EXPIRY 06/26..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **CVV •••** (243:6526)
  - Text: "CVV •••..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **•••• 2341** (243:6608)
  - Text: "•••• 2341..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **ICICI a/c - xxx23** (243:6610)
  - Text: "ICICI a/c -  xxx23..."
  - Has 3 style segments
  - Fix: Use single `<Text>` with nested spans
- **rishabh@•••** (243:6611)
  - Text: "rishabh@•••..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **•••• 2341** (243:6630)
  - Text: "•••• 2341..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **EXPIRY 06/26** (243:6632)
  - Text: "EXPIRY 06/26..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans
- **ICICI a/c - xxx23** (243:6633)
  - Text: "ICICI a/c -  xxx23..."
  - Has 3 style segments
  - Fix: Use single `<Text>` with nested spans
- **Choose a  Payment Method** (243:6701)
  - Text: "Choose a   Payment Method..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans

### Button Fill Issues (6)
*Buttons with specific fill colors - verify RN backgroundColor matches*

- **Rectangle 140** (I243:6507;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **Rectangle 140** (I243:6651;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **Rectangle 140** (I243:6686;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **Rectangle 54** (243:6687)
  - Button text: "Rectangle 54"
  - Figma fill: `#000000`
  - Action: Verify button has backgroundColor: '#000000' or equivalent design token
- **Rectangle 53** (243:6689)
  - Button text: "Rectangle 53"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token
- **Rectangle 140** (I243:6727;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token

### Position Issues (2)
*Elements with specific offsets requiring absolute positioning*

- **Paying with:** (243:6512)
  - Relative position: left=64.0, top=90.0
  - Fix: Add absolute positioning with exact coordinates
- **Frame 2095586316** (243:6657)
  - Relative position: left=180.5, top=432.0
  - Fix: Add absolute positioning with exact coordinates

## Consolidated Fixes

### Critical Issues (Fix Immediately)

#### Modal Presentation (Layering & Blur) in `app/screens/HomeEmptyState.tsx`
**Lines:** N/A
**Problem:** The bottom sheet modal requires a blurred overlay with correct z-index layering to match the design. The current implementation likely renders components sequentially, missing the blur effect and proper stacking order, which breaks the intended user experience.
**Figma:** Correct z-index stack (Base -> Bar -> Overlay -> Sheet) with a blur(8) overlay. | **Current:** Likely incorrect layering and missing blur effect.

```typescript
// Before:
/* Likely sequential rendering without absolute positioning and zIndex, and a simple View for the overlay */
<View style={{flex: 1}}>
  <MainContent />
  <FloatingBar />
  {isModalVisible && <View style={styles.overlay} />}
  {isModalVisible && <BottomSheet />}
</View>

// After:
/* Use absolute positioning, zIndex, and a BlurView component for the overlay */
import { BlurView } from '@react-native-community/blur'; // or equivalent library

<View style={{flex: 1}}>
  <MainContent />
  {/* Floating bar needs to be above content but below overlay */}
  <FloatingBar style={{ position: 'absolute', bottom: 0, zIndex: 5 }} />
  {isModalVisible && (
    <>
      <BlurView 
        style={{ ...StyleSheet.absoluteFillObject, zIndex: 10 }}
        blurType="dark"
        blurAmount={8} // Corresponds to Figma's radius
      />
      {/* Bottom sheet must have the highest zIndex */}
      <BottomSheet style={{ zIndex: 20 }} />
    </>
  )}
</View>
```

### Major Issues

- **Text Structure** in `src/components/243-6490/PaymentSheet.tsx`: Multiple text elements in the design use different styles (e.g., colors) within a single line. These must be rendered using a single parent `<Text>` component with nested `<Text>` children for each style segment. Implementing them as separate `<Text>` components or using line breaks will cause incorrect layout and wrapping.
  - Fix: `/* Correct implementation for 'Your rent is due in 10 days' */
<Text style={styles.baseText}>
  <Text style={{color: colors.neutral[100]}}>Your rent is due{"\n"}</Text>
  <Text style={{color: colors.brand[500]}}>in 10 days</Text>
</Text>

/* Apply this pattern to all multi-styled text instances, such as:
- "₹ 32,500"
- "•••• 2341"
- "EXPIRY 06/26"
- "CVV •••"
- "ICICI a/c - xxx23"
- "rishabh@•••"
- "Choose a Payment Method" */`
- **ScrollView.contentContainerStyle.paddingBottom** in `app/screens/HomeEmptyState.tsx`: The main scrollable content is obscured by a floating bottom bar of height 118px. To ensure all content is accessible, the ScrollView's content container needs a bottom padding equal to the height of the bar.
  - Fix: `<ScrollView contentContainerStyle={{ paddingBottom: 118 }}>
  {/* ... content ... */}
</ScrollView>`
- **Button.backgroundColor** in `src/components/243-6490/PaymentSheet.tsx`: Multiple buttons within the payment sheet have incorrect background colors. The design specifies `#4D4D4D` for secondary/inactive states and `#000000` for primary actions.
  - Fix: `/* Ensure buttons use the correct background colors from the design tokens */

/* For buttons with #4D4D4D fill */
<TouchableOpacity style={{ backgroundColor: colors.black[400] }}>
  {/* ... */}
</TouchableOpacity>

/* For buttons with #000000 fill */
<TouchableOpacity style={{ backgroundColor: '#000000' }}>
  {/* ... */}
</TouchableOpacity>`
- **position** in `app/screens/HomeEmptyState.tsx`: The text element "Paying with:" is positioned relative to its parent container using specific offsets. This requires absolute positioning to match the Figma design.
  - Fix: `style={{ position: 'absolute', left: 64, top: 90 }}`
- **position** in `app/screens/HomeEmptyState.tsx`: A container element (Figma: "Frame 2095586316") is positioned with specific offsets relative to its parent. This requires absolute positioning to match the design.
  - Fix: `style={{ position: 'absolute', left: 180.5, top: 432 }}`
