# Pixel-Perfect Feedback Report

**Screen:** Splash / get-started --animation
**Figma ID:** 1-28055
**Route:** /(auth)/splash
**Generated:** 2026-02-02T00:08:20.212Z

## Summary

| Metric | Value |
|--------|-------|
| Total Issues | 7 |
| Critical | 4 |
| Major | 2 |
| Minor | 1 |
| Files Affected | 1 |

## Batch 0: Structural Analysis (Deterministic)

These issues were detected by analyzing Figma data directly - **visual comparison cannot catch these**:

### Text Structure Issues (2)
*Text that should be single line with mixed colors, NOT multiple lines*

- **Main Heading** (1:28066)
  - Text: "Make  your rent  work for you→..."
  - Has 4 style segments
  - Fix: Use single `<Text>` with nested spans
- **Login Text** (1:28070)
  - Text: "Already a user? Log in..."
  - Has 2 style segments
  - Fix: Use single `<Text>` with nested spans

### Button Fill Issues (1)
*Buttons with specific fill colors - verify RN backgroundColor matches*

- **Rectangle 140** (I1:28069;137:37)
  - Button text: "Rectangle 140"
  - Figma fill: `#4D4D4D`
  - Action: Verify button has backgroundColor: '#4D4D4D' or equivalent design token

### Position Issues (1)
*Elements with specific offsets requiring absolute positioning*

- **image 149** (237:2727)
  - Relative position: left=-463.0, top=-747.0
  - Fix: Add absolute positioning with exact coordinates

## Consolidated Fixes

### Critical Issues (Fix Immediately)

#### Layout Structure in `app/(auth)/get-started.tsx`
**Lines:** N/A
**Problem:** The screen's vertical spacing is incorrect. The main content should be in a container that is absolutely positioned 205px from the top with a fixed height of 613px. Using `justifyContent: 'space-between'` on this container will correctly push the heading to the top and the buttons to the bottom, creating the large, intentional empty space in the middle.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<SafeAreaView style={styles.container}>
  {/* Content is likely here, with simple flexbox spacing */}
</SafeAreaView>

// After:
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: _designTokens.colors.black[700],
  },
  contentWrapper: {
    position: 'absolute',
    top: 205,
    left: 0,
    right: 0,
    height: 613,
    justifyContent: 'space-between',
    paddingBottom: _designTokens.spacing.huge, // 64px
  },
  // ... other styles
});

// In component render:
<SafeAreaView style={styles.container}>
  {/* Background images go here */}
  <View style={styles.contentWrapper}>
    {/* Top content (heading) and bottom content (buttons) go here */}
  </View>
</SafeAreaView>
```

#### Text Structure in `app/(auth)/get-started.tsx`
**Lines:** N/A
**Problem:** The main heading 'Make your rent work for you→' is a single line in Figma with multiple colors. It must be rendered using a single `<Text>` component with nested `<Text>` components for the different styles to prevent incorrect line breaks and ensure proper styling.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<Text>Make</Text>
<Text>your rent</Text>
<Text>work for you→</Text>

// After:
<Text style={styles.heading}>
  Make your rent work for you
  <Text style={styles.arrow}>→</Text>
</Text>

// styles.heading would contain the base text style (e.g., color: white)
// styles.arrow would contain the different style (e.g., color: brand[500])
```

#### Text Structure in `app/(auth)/get-started.tsx`
**Lines:** N/A
**Problem:** The login prompt 'Already a user? Log in' is a single line in Figma with two different styles. It must be rendered as a single `<Text>` component with a nested `<Text>` component to ensure it flows correctly as one line and is stylistically accurate.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
<Text>Already a user?</Text>
<Text>Log in</Text>

// After:
<Text style={styles.loginPromptBase}>
  Already a user?{' '}
  <Text style={styles.loginPromptLink}>Log in</Text>
</Text>

// styles.loginPromptBase would contain the base text style (e.g., color: neutral[500])
// styles.loginPromptLink would contain the link style (e.g., color: brand[500])
```

#### backgroundColor in `app/(auth)/get-started.tsx`
**Lines:** N/A
**Problem:** The 'Get Started' button's background color is incorrect. Figma specifies a fill of #4D4D4D, which corresponds to the `colors.black[400]` design token.
**Figma:** undefined | **Current:** undefined

```typescript
// Before:
backgroundColor: _designTokens.colors.brand[500], // Or other incorrect value

// After:
backgroundColor: _designTokens.colors.black[400],
```

### Major Issues

- **Background Layers** in `app/(auth)/get-started.tsx`: The screen background is missing its texture layer. A large image ('image 149' in Figma) should be added and absolutely positioned at `top: -747, left: -463` with an opacity of `0.08` to add visual depth. This fix combines a structural positioning issue with a visual one.
  - Fix: `<SafeAreaView style={styles.container}>
  <Image
    source={require('@/assets/images/splash-texture.png')} // Ensure image path is correct
    style={{
      position: 'absolute',
      top: -747,
      left: -463,
      width: 1319, // from Figma
      height: 2346, // from Figma
      opacity: 0.08,
    }}
  />
  {/* ... rest of screen content ... */}
</SafeAreaView>`
- **paddingHorizontal** in `app/(auth)/get-started.tsx`: The main content blocks (top text and bottom buttons) are missing the correct horizontal padding. They should have a `paddingHorizontal` of 48px (`spacing.xxxl`) to create the wide side margins seen in the design.
  - Fix: `const styles = StyleSheet.create({
  topContentContainer: {
    paddingHorizontal: _designTokens.spacing.xxxl, // 48px
    // ... other styles
  },
  bottomContentContainer: {
    paddingHorizontal: _designTokens.spacing.xxxl, // 48px
    // ... other styles
  },
});`

### Minor Issues

- **Animation** in `app/(auth)/get-started.tsx`: The screen is static on load. The design name ('--animation') implies that elements should animate in. Implementing a staggered fade-in/slide-up animation for the content would match the design intent and improve user experience.